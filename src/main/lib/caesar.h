#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* encrypt(const char*, int);
char* decrypt(const char*, int);
void output(const char*, const char*);
void encrypt_output(const char*, int, const char*);
void decrypt_output(const char*, int, const char*);